## Completed Tasks
- [x] 1.34 Create Grafana adapters dashboard JSON - pending commit

## Current Task
Awaiting next task

## Learnings
- Grafana dashboard JSON uses schemaVersion 39, templated datasource UIDs with `${DS_PROMETHEUS}` via `__inputs`
